# Serve images only — never execute anything as PHP from an upload directory,
# even though uploads are already MIME-validated and size-limited elsewhere.
<FilesMatch "\.(?:php|phtml|php3|php4|php5|phar)$">
    Require all denied
</FilesMatch>
Options -Indexes
